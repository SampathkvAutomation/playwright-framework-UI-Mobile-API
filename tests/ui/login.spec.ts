// import { test, expect } from '@playwright/test';
// import { LoginPage } from '../../pages/Loginpage';
// import {UIActions} from '../../ultils/UIActions'

//    const loginPage = new LoginPage(page);
//   await loginPage.OpenLoginPage();
//   await loginPage.login('user', 'pass'); // Replace with valid credentials
//   const welcomeText = await page.textContent('.app_logo'); // Adjust selector
//   expect(welcomeText).toContain('Swag Labs');
//   const uiactions = new UIActions();
//   uiactions.takeScreenShotAndAttachToreport(page,"Homepage");


//   const [request] = await Promise.all([
//     page.waitForEvent('request'),
//     page.click('#fetchData')
// ]);

// request.postData();


// const [response] = await Promise.all([
//   page.waitForEvent('response', res => res.url().includes('/api/data')),
//   page.click('#loadData')

// ]);

// // await   page.request("url ",
// //   {

// //   }
// // );
// expect(page).toHaveURL("  ");

// // const selectedOption = await page.locator('#dropdown').text
// });
